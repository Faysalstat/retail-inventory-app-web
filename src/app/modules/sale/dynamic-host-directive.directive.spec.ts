import { DynamicHostDirectiveDirective } from './dynamic-host-directive.directive';

describe('DynamicHostDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicHostDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
